//! Attendance seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial Attendance data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ATTENDANCE SEEDER
// ============================================================================

/// Seeder for attendance
pub struct SeedAttendanceSeeder;

impl SeedAttendanceSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedAttendanceSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedAttendanceSeeder {
    fn name(&self) -> &'static str {
        "SeedAttendanceSeeder"
    }

    fn order(&self) -> i32 {
        1
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if hr.attendances table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM hr.attendances")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/attendance_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM hr.attendances")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

